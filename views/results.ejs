<h1>This is the results page</h1>

<a href="/"><button>Search Again</button></a>

<% data.Search.forEach((e) => { %>
   <li> <%= e.Title %> </li>
<% }) %>

<br><br>

<% data.Search.forEach((e) => { %>
   <% if (e.Poster !== 'N/A') { %>
      <img src=" <%= e.Poster %> " >
   <% } %>
<% }) %>
